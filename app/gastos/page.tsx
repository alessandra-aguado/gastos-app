export default function Page() {
  return (
    <div className="max-w-5xl mx-auto px-6 py-8">
      <h1 className="text-2xl font-semibold">🛒 Gastos</h1>
      <p className="text-muted text-sm mt-2">Detalle de tus gastos por categoría y subcategoría.</p>
      <div className="mt-8 border border-dashed border-border rounded-2xl p-10 text-center text-muted text-sm">
        Próxima fase de construcción — placeholder para que la navegación ya funcione de punta a punta.
      </div>
    </div>
  );
}
